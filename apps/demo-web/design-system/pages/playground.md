# Playground Override

- Layout is a two-zone workbench: left control deck, right fixed result deck.
- Priority order:
  - model / device / LoRA
  - text + voice conditioning
  - reference audio + prompt assist
  - advanced inference recipe
- Streaming is visualized as a terminal-like timeline, not a plain text list.
- Result deck keeps audio, waveform, mel, ASR, and notes visible without tab switching.
