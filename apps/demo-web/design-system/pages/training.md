# Training Override

- Training uses a split between light control surfaces and a dark terminal log zone.
- Capability and risk notices must be explicit for CPU, MPS, and CUDA.
- Output path, log path, and checkpoint gallery are treated as operational artifacts.
- The page should feel like a control room, not a generic settings form.
